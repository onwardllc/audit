name: test

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: test
        uses: actions/github-script@v8
        env:
          SHA: ${{ github.sha }}
        with:
          script: |
            echo ${SHA}
